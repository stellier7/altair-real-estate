import type { Property } from "@/lib/properties/types";

/** Demo records removed — live catalog loads from `src/data/properties.json` via `propertyRepository`. */
export const properties: Property[] = [];
